{% extends 'base.html.twig' %}

{% block body %}
    <h1>Productlines</h1>

    <table>
        <tbody>
            <tr>
                <th>Textdescription</th>
                <td>{{ productline.textdescription }}</td>
            </tr>
            <tr>
                <th>Htmldescription</th>
                <td>{{ productline.htmldescription }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ productline.image }}</td>
            </tr>
            <tr>
                <th>Productline</th>
                <td>{{ productline.productline }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('productlines_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('productlines_edit', { 'id': productline.productline }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
